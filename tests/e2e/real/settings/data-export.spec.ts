import { test } from '../../../fixtures/real.fixture';

test.describe('Data export', () => {
  test.skip('export CSV triggers a file download', async ({ loggedInPage: _loggedInPage }) => {
    // TODO: implement — navigate to settings, click export CSV, assert download event fires
  });
});
