import { test } from '../../../fixtures/real.fixture';

test.describe('Transaction list and filtering', () => {
  test.skip('filter by type shows correct transaction count', async ({
    loggedInPage: _loggedInPage,
  }) => {
    // TODO: implement — seed mixed transactions then assert filter counts per type
  });
});
