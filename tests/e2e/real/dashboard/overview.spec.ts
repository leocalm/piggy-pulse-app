import { test } from '../../../fixtures/real.fixture';

test.describe('Dashboard overview', () => {
  test.skip('dashboard shows spent, income, and net position values', async ({
    loggedInPage: _loggedInPage,
  }) => {
    // TODO: implement — seed transactions and verify dashboard cards display expected values
  });
});
