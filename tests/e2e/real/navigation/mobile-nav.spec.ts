import { test } from '../../../fixtures/real.fixture';

test.describe('Mobile bottom navigation', () => {
  test.skip('bottom bar navigates to the correct pages', async ({
    loggedInPage: _loggedInPage,
  }) => {
    // TODO: implement — set mobile viewport, tap each bottom nav item, verify URL
  });
});
