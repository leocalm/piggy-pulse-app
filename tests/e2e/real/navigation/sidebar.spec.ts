import { test } from '../../../fixtures/real.fixture';

test.describe('Sidebar navigation', () => {
  test.skip('sidebar links navigate to the correct pages', async ({
    loggedInPage: _loggedInPage,
  }) => {
    // TODO: implement — click each sidebar nav link and verify the URL and page heading
  });
});
