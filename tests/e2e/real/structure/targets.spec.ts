import { test } from '../../../fixtures/real.fixture';

test.describe('Budget targets', () => {
  test.skip('set budget target for a category persists the amount', async ({
    loggedInPage: _loggedInPage,
  }) => {
    // TODO: implement — create a category and period, then set a budget target and verify it is stored
  });
});
